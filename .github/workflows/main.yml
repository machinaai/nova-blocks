
name: Umi-ui
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build_umi_ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v2.1.4
      - run: |
          ls
          npm i
          npm run lint-ui


