import { Card } from 'antd';
import React from 'react';

import styles from '../style.less';

const OfflineData = () => <Card className={styles.offlineCard} style={{ marginTop: 32 }} />;

export default OfflineData;
